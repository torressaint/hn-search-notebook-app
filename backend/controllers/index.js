const searchResult = require("./searchresult-controller");
const searchQuery = require("./searchquery-controller");
const searchNotebook = require("./searchnotebook-controller");
const statistic = require("./statistic-controller");

module.exports = {
  searchResult,
  searchQuery,
  searchNotebook,
  statistic,
};
